const User = require('./User');
// const Blog = require('./Blog');

// add any associations here (hasMany, belongsTo, etc)


module.exports = { User, Blog }; //add other models here separated by a comma and a space